spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/synapse_dev
    username: dev_user
    password: dev_password

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
  
jwt:
  issuer: synapse
  access-token-ttl: 15m
  secret: ${JWT_SECRET}