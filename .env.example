JWT_SECRET=
GEMINI_API_KEY=
GROQ_API_KEY=
RESEND_API_KEY=

# Optional overrides. Both default to the local frontend and must point at the
# deployed frontend in production, because emailed links open these pages.
# EMAIL_VERIFICATION_URL=http://localhost:5173/verify-email
# PASSWORD_RESET_URL=http://localhost:5173/reset-password
